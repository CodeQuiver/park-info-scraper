<h1>Arlington Parks &amp; Recreation Highlights</h1>
<h2>Saved Articles</h2>

<a href="/" class='btn btn-info'>Return to Homepage</a>

<hr />
<div class="article-box">
    <!-- script will insert all articles here -->
</div>

{{!-- add the buttons to go with following code from js file
buttons- "permanently delete article", "remove from saved articles", "view comments", "save comment", "delete comment" */
$(document).on("click", ".btn.delete", handleArticleDelete);
$(document).on("click", ".btn.unsave", handleArticleUndoSave);
$(document).on("click", ".btn.comments", handleArticleComments);
$(document).on("click", ".btn.save", handleCommentSave);
$(document).on("click", ".btn.comment-delete", handleCommentDelete); --}}